modLoader="javafml"
loaderVersion="[47,)"
license="${mod_license}"
issueTrackerURL="https://github.com/Angais/minedit/issues"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
displayURL="https://github.com/Angais/minedit"
credits="Uses Minecraft Forge, Mozilla Rhino, OpenRouter, OpenAI Codex local bridge, Cursor local bridge, and optional Hermes runs integration."
authors="Angais"
description='''
Prompt, build, stage, and edit Minecraft structures with OpenRouter models, local agent bridges, or Hermes.
'''

[[dependencies.${mod_id}]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"
